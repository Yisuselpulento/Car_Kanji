import { useState } from 'preact/hooks'

export const ButtonOpenModal = () => {
  return (
    <button class='py-4 px-6 w-full bg-primary hover:bg-hoverPrimary text-white font-extrabold'>
      COMPRAR
    </button>
  )
}
